alembic==1.18.5
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
cffi==2.1.0
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
cryptography==50.0.0
fastapi==0.141.1
greenlet==3.5.4
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
Mako==1.3.12
MarkupSafe==3.0.3
packaging==26.2
pluggy==1.6.0
psycopg2-binary==2.9.12
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
Pygments==2.20.0
pytest==9.1.1
python-dotenv==1.2.2
PyYAML==6.0.3
requests==2.34.2
SQLAlchemy==2.0.51
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.0
watchfiles==1.2.0
websockets==17.0

# --- setu_ai_service embedding dedup (now in use, SETU_AI_EMBEDDING_DEDUP_ENABLED=true) ---
sentence-transformers
numpy

# --- setu_ai_service Gemini path -- still NOT needed (SETU_AI_GEMINI_ENABLED stays false) ---
# google-genai       -- only if SETU_AI_GEMINI_ENABLED=true

# --- Email OTP auth (app/routers/auth.py) ---
# email-validator backs pydantic's EmailStr; without it, importing
# app/schemas/auth.py raises ImportError at startup.
email-validator

# --- Voice SOS ingest (app/routers/voice.py) ---
# python-multipart is REQUIRED for FastAPI's UploadFile/Form handling.
# Without it, FastAPI raises at import time on any route using File().
python-multipart

# openai-whisper powers voice transcription (setu_ai_service/service/voice_service.py).
# NOTE: it ALSO needs ffmpeg installed at the SYSTEM level -- ffmpeg is not
# a pip package. On Ubuntu: sudo apt install ffmpeg
# On a fresh Render instance neither is present, so /ingest/voice will
# return 503 there until this is provisioned. Left uncommented because
# voice SOS is now a shipped feature, not a roadmap item -- but be aware
# this pulls in torch and is a large install.
openai-whisper
